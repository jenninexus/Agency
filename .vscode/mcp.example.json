{
  "_comment": "VS Code MCP server entry only. Copy to .vscode/mcp.json for local use. Agent registry lives in ../mcp.example.json; copy that to ../mcp.json and customize locally.",
  "servers": {
    "agency": {
      "type": "stdio",
      "command": "node",
      "args": ["${workspaceFolder}/scripts/mcp-server.js"]
    }
  }
}
